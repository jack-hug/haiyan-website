[Unit]
Description=uWSGI instance to serve unice-website
After=network.target

[Service]
User=ubuntu
Group=www-data

WorkingDirectory=/home/precision
Environment="PATH=/home/precision/venv/bin"
ExecStart=/home/precision/venv/bin/uwsgi --ini unice.ini

[Install]
WantedBy=multi-user.target
